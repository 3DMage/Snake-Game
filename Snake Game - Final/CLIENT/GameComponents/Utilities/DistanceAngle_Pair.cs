namespace GameComponents
{
    public class DistanceAngle_Pair
    {
        // Distance of the line.
        public float distance;

        // Angle of the line.
        public float angle;

        // Constructor.
        public DistanceAngle_Pair(float distance, float angle)
        {
            this.distance = distance;
            this.angle = angle;
        }
    }
}
