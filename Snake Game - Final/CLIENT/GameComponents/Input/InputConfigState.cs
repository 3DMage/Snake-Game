namespace GameComponents
{
    // Label for states for input configuration.
    public enum InputConfigState
    {
        VALID,
        ALREADY_MAPPED,
        ITSELF,
        CANCELED,
        NOTHING
    }
}
