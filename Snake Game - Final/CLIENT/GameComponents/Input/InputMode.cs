namespace GameComponents
{
    // Label indicating how input should be handled.
    public enum InputMode
    {
        DISABLED_MODE,
        COMMAND_MODE,
        TYPE_MODE,
        INPUT_CONFIG_MODE
    }
}
